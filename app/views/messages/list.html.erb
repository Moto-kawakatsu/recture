<div class="class-number">1-1</div>
<div class="lists">
<% @users.each do |user|%>
    <div class="list1-1 hidden">
        <% if user.grade == 1 && user.class_number == 1%>
        <%=link_to new_user_diary_path(user) do %>
        No.<%= user.student_number%>
        <%= user.name%>
        （<%=user.current_sign_in_at.strftime('%m月%d日 %H:%M')%>）
        <% end %>
        <% end %>
    </div>
    <% end %>
</div>
</div>

<div class="class-number">1-2</div>
<div class="lists">
<% @users.each do |user|%>
    <div class="list1-1 hidden">
        <% if user.grade == 1 && user.class_number == 2%>
        <%=link_to new_user_diary_path(user) do %>
        No.<%= user.student_number%>
        <%= user.name%>
        <%end%>
        <% end %>
    </div>
    <% end %>
</div>
</div>

<div class="class-number">1-3</div>
<div class="lists">
<% @users.each do |user|%>
    <div class="list1-1 hidden">
        <% if user.grade == 1 && user.class_number == 3%>
        <%=link_to new_user_diary_path(user) do %>
        No.<%= user.student_number%>
        <%= user.name%>
        <%end%>
        <% end %>
    </div>
</div>
</div>


</div>
<% end %>

<script>
    let c1 = document.querySelectorAll('.class-number');
    let members = document.querySelectorAll('.list1-1');
    c1 = Array.from(c1);
    members = Array.from(members);

    c1.forEach(function(list) {
        list.addEventListener('click', function(){
        let members = document.querySelectorAll('.list1-1');
        let members1 = Array.from(members);
        for(const member of members) {
        if (member.getAttribute('style') == 'display:block;') {
            member.removeAttribute('style','display:block;')
        } else {
            member.setAttribute('style','display:block;')
        }
        }
    })
    })

</script>