<div class="chat-header2">
<div class="diary-title">学習<span class="small-no">の</span>記録</div>
  <div class="left-header">
    <div class="header-title">
      <%# <%= @room.name %>
    </div>
  </div>
  <div class="right-header">
    <div class="header-button">
    </div>
  </div>
</div>

<div class="messages2">
   <div class="message2">
  <div class="upper-message">
    <div class="message-user">
      <%# <%= @diary.user.name %>
    </div>
    <div class="message-date">
      <%=  l @diary.created_at %>
    </div>
    <div class="message-date">

    </div>
    <div>
    <% if current_user.genre == "教員" %>
      <p>
        <%= button_to user_diary_path(@user.id, @diary.id) ,method: :delete, class: "delete-button" do %>
        <i class="fas fa-trash"></i>
        <% end %>
      </p>
      <% elsif user_signed_in? && current_user.id == @diary.user_id %>
      <p>
        <%= button_to user_diary_path(@user.id, @diary.id), method: :delete, class: "delete-button" do %>
        <i class="fas fa-trash"></i>
        <% end %>
      </p>
    <% end %>
    </div>
  </div>
  <div class="lower-message">
    <div class="message-content2">
      <%=link_to @diary.writing, new_message_comment_path(@diary) %>
    </div>
     <%= link_to  new_message_comment_path(@diary) do %>
     <%end%>
  </div>
</div>
</div>

<div class="message3">
aaaaaa
</div>

<%= form_with model:[@user,@diary], class: 'form2', local: true do |f| %>
  <div class="form-input">
    <%= f.text_field :reply, class: 'form-message', placeholder: 'type a message' %>
  </div>

  <%= f.submit '送信', class: 'form-submit' %>
<% end %>